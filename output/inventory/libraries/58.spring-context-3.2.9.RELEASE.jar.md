# spring-context-3.2.9.RELEASE.jar 분석 보고서

## 1. 기본 정보

| 항목       | 내용                               |
| -------- | -------------------------------- |
| 라이브러리명   | Spring Context                   |
| 파일명      | spring-context-3.2.9.RELEASE.jar |
| 버전       | 3.2.9.RELEASE                    |
| 제조사/오픈소스 | Spring Framework / Open Source   |
| 라이선스     | Apache License 2.0               |
| 분류       | Spring Application Context       |
| 전환 대상 여부 | 대상                               |
| 위험도      | 높음                               |

---

## 2. 라이브러리 개요

### 주요 기능

* ApplicationContext 제공
* Bean 관리 및 의존성 주입
* Component Scan 지원
* 이벤트(Event) 처리
* MessageSource 및 Resource 관리

### 사용 목적

`spring-context`는 Spring Framework의 핵심 컨테이너 라이브러리로 ApplicationContext를 제공하며 Bean 생성, 의존성 주입, 이벤트 처리, 리소스 관리 등의 기능을 담당한다.

전자정부프레임워크 기반 시스템에서는 대부분의 Spring Bean 관리가 `spring-context`를 중심으로 수행된다.

### 대표 사용 예시

```java id="tqz8g3"
ApplicationContext context =
    new ClassPathXmlApplicationContext(
        "egovframework/spring/context-common.xml"
    );

UserService service =
    context.getBean(UserService.class);
```

또는

```xml id="d6rq1z"
<context:component-scan
    base-package="egovframework" />

<context:annotation-config />
```

---

## 3. AS-IS 사용 현황

### 프로젝트 내 사용 여부

* 사용

### 확인 방법

```bash id="x8z9tt"
find . -name "spring-context*.jar"

grep -R "ApplicationContext" src

grep -R "ClassPathXmlApplicationContext" src

grep -R "context:" src/main/resources

grep -R "component-scan" src/main/resources

grep -R "annotation-config" src/main/resources
```

### 예상 사용 위치

* DAO
* Service
* Controller
* Batch
* 기타

---

## 4. eGovFrame 4.3 영향도 분석

### 전환 대상 여부

* 대상

### 전환 사유

`spring-context-3.2.9.RELEASE.jar`는 Spring Framework 3.2 계열의 핵심 라이브러리이다.

eGovFrame 4.3은 Spring Framework 5.3.37 기반으로 구성되므로 Spring Core 계열 라이브러리 전체 업그레이드가 필요하다.

특히 `spring-context`는 `spring-core`, `spring-beans`, `spring-expression`, `spring-aop`, `spring-tx`와 긴밀하게 연계되어 동작하므로 단독 교체는 불가능하다.

ApplicationContext 초기화, Bean 등록, Component Scan, Event 처리 기능에 직접적인 영향을 미치므로 전환 시 영향도가 매우 높다.

### 영향 범위

| 구분      | 영향 |
| ------- | -- |
| 소스      | 높음 |
| 설정파일    | 높음 |
| pom.xml | 높음 |
| 운영환경    | 중간 |

---

## 5. 전환 시 검토사항

### 변경 필요 사항

* spring-context 3.2.9 → 5.3.37 업그레이드
* Spring Core 계열 라이브러리 버전 통일
* XML 설정파일 검증
* Component Scan 설정 검증
* Annotation 기반 설정 검증
* ApplicationContext 초기화 검증
* Bean 등록 및 DI 검증
* Event 처리 기능 검증

### 자동 변환 가능 여부

| 구분          | 가능 여부 |
| ----------- | ----- |
| OpenRewrite | 일부 가능 |
| Python Rule | 가능    |
| LLM 보정      | 필요    |

### 예상 난이도

* 높음

---

## 6. 버전 업그레이드 검토

### AS-IS 버전

```text
spring-context-3.2.9.RELEASE.jar
```

### TO-BE 버전

```text
spring-context-5.3.37.jar
```

### 주요 변경사항

* Spring Framework 3.2 → 5.3 업그레이드
* ApplicationContext 내부 구조 개선
* Component Scan 처리 개선
* JDK 8 이상 환경 적용
* Spring Core 계열 라이브러리 버전 정합성 확보
* Event 처리 기능 개선

### 소스 수정 여부

* 일부 수정 필요

---

## 7. 위험요소

### 보안

* Spring 3.2 계열 기술지원 종료
* 최신 보안 패치 미적용
* 공개 취약점 대응 불가

### 운영

* ApplicationContext 초기화 실패 가능
* Bean 생성 실패 가능
* Component Scan 오류 가능
* Event 처리 오류 가능
* Spring 라이브러리 버전 충돌 가능

### 호환성

* JDK 영향 있음
* Spring 영향 높음
* eGovFrame 영향 높음

---

## 8. 검토 결과

| 항목       | 결과       |
| -------- | -------- |
| 전환 대상 여부 | 대상       |
| 자동 변환    | 일부 가능    |
| 소스 수정    | 일부 수정 필요 |
| 설정 수정    | 일부 수정 필요 |
| 버전 업그레이드 | 필요       |
| 제거 가능성   | 없음       |
| 위험도      | 높음       |

---

## 9. 최종 판정

### 판정

```text
전환대상여부 :
대상

조치방안 :
spring-context-3.2.9.RELEASE.jar 제거 후
spring-context-5.3.37.jar 적용

spring-core
spring-beans
spring-expression
spring-aop
spring-tx
등 Spring Core 계열 라이브러리와 함께 버전 통일

ApplicationContext 기동 검증
Bean 생성 검증
Component Scan 검증
Event 처리 기능 검증 수행

자동변환 :
일부 가능

소스수정 :
일부 수정 필요

위험도 :
높음
```

### 비고

* eGovFrame 4.3 전환 시 반드시 업그레이드해야 하는 핵심 라이브러리이다.
* `spring-context`는 Spring Container의 중심 모듈로 시스템 전체에 영향을 준다.
* `context:component-scan`, `context:annotation-config`, `ApplicationContext` 관련 설정은 대부분 유지 가능하나 실제 동작 검증이 필요하다.
* `spring-context` 단독 전환이 아닌 `spring-core`, `spring-beans`, `spring-expression`, `spring-aop`, `spring-tx`와 함께 일괄 전환해야 한다.
* 전환 후 ApplicationContext 기동 테스트, 주요 업무 기능 테스트, 트랜잭션 및 DI 동작 검증을 반드시 수행해야 한다.

---

#### (참고) spring-context 3.2.9.RELEASE → 5.3.37 AS-IS / TO-BE 수정 가이드

##### (1) Java 소스 수정 여부 요약

| 구분                                                     |  수정 여부 | 내용                                          |
| ------------------------------------------------------ | -----: | ------------------------------------------- |
| `ApplicationContext` 사용                                | 대부분 없음 | 일반적인 Context 사용 코드는 유지 가능                   |
| `ClassPathXmlApplicationContext`                       | 대부분 없음 | 기존 XML 로딩 방식은 대부분 유지 가능                     |
| `WebApplicationContext`                                | 대부분 없음 | 웹 컨텍스트 사용 방식 유지 가능                          |
| `@Component`, `@Service`, `@Repository`, `@Controller` |     없음 | Component Scan 대상 Annotation 유지 가능          |
| `@Autowired`, `@Resource`                              |     없음 | 의존성 주입 Annotation 유지 가능                     |
| XML `<context:component-scan>`                         | 대부분 없음 | 설정 구조 유지 가능                                 |
| XML `<context:annotation-config>`                      | 대부분 없음 | 설정 구조 유지 가능                                 |
| Property Placeholder 설정                                |  검토 필요 | 구형 `PropertyPlaceholderConfigurer` 사용 여부 확인 |
| MessageSource 설정                                       |  검토 필요 | 다국어/메시지 설정 동작 확인                            |
| 커스텀 ApplicationContext 초기화 코드                          |  검토 필요 | 직접 Context를 생성하는 코드는 수동 검토                  |

---

##### (2) 전환 대상 요약

| 항목                               | AS-IS                              | TO-BE                                   | OpenRewrite |
| -------------------------------- | ---------------------------------- | --------------------------------------- | ----------- |
| Maven 의존성                        | `spring-context 3.2.9.RELEASE`     | `spring-context 5.3.37`                 | 가능          |
| WEB-INF/lib                      | `spring-context-3.2.9.RELEASE.jar` | `spring-context-5.3.37.jar` 또는 Maven 관리 | 가능          |
| Spring 계열 버전                     | Spring 3.2.9 혼재                    | Spring 5.3.37 계열 통일                     | 가능          |
| `ApplicationContext`             | 유지                                 | 유지                                      | 수정 불필요      |
| `ClassPathXmlApplicationContext` | 유지                                 | 유지                                      | 수정 불필요      |
| `@Component` 계열                  | 유지                                 | 유지                                      | 수정 불필요      |
| `<context:component-scan>`       | 유지                                 | 유지, 동작 검증                               | 대부분 수정 불필요  |
| `<context:annotation-config>`    | 유지                                 | 유지, 동작 검증                               | 대부분 수정 불필요  |
| Property Placeholder             | 구형 설정 가능                           | Spring 5.3 환경 동작 검증                     | 일부 가능       |
| MessageSource                    | 기존 설정                              | 유지, 메시지 출력 검증                           | 수동 검토       |
| XML Schema                       | 버전 고정 XSD 가능                       | 버전 없는 XSD 권장                            | 가능          |

---

##### (3) Maven 의존성 변경

###### AS-IS

```xml
<dependency>
    <groupId>org.springframework</groupId>
    <artifactId>spring-context</artifactId>
    <version>3.2.9.RELEASE</version>
</dependency>
```

###### TO-BE

```xml
<dependency>
    <groupId>org.springframework</groupId>
    <artifactId>spring-context</artifactId>
    <version>5.3.37</version>
</dependency>
```

###### OpenRewrite 적용 가능 여부

```text
가능
```

---

##### (4) WEB-INF/lib 직접 Jar 변경

###### AS-IS

```text
WEB-INF/lib/spring-context-3.2.9.RELEASE.jar
```

###### TO-BE

```text
WEB-INF/lib/spring-context-5.3.37.jar
```

또는 Maven 기반 관리로 전환한다.

```xml
<dependency>
    <groupId>org.springframework</groupId>
    <artifactId>spring-context</artifactId>
    <version>5.3.37</version>
</dependency>
```

###### OpenRewrite 적용 가능 여부

```text
가능
```

---

##### (5) Spring Framework 계열 버전 통일

###### AS-IS

```text
spring-core-3.2.9.RELEASE.jar
spring-beans-3.2.9.RELEASE.jar
spring-context-3.2.9.RELEASE.jar
spring-expression-3.2.9.RELEASE.jar
spring-aop-3.2.9.RELEASE.jar
spring-tx-3.2.9.RELEASE.jar
```

###### TO-BE

```text
spring-core-5.3.37.jar
spring-beans-5.3.37.jar
spring-context-5.3.37.jar
spring-expression-5.3.37.jar
spring-aop-5.3.37.jar
spring-tx-5.3.37.jar
```

###### OpenRewrite 적용 가능 여부

```text
가능
```

###### 비고

`spring-context`는 `spring-core`, `spring-beans`, `spring-expression`과 함께 동작하므로 Spring Framework 계열 전체 버전을 동일하게 맞춰야 한다.

---

##### (6) ApplicationContext 사용 코드 유지

###### AS-IS

```java
ApplicationContext context =
    new ClassPathXmlApplicationContext("egovframework/spring/context-common.xml");

UserService userService =
    context.getBean("userService", UserService.class);
```

###### TO-BE

```java
ApplicationContext context =
    new ClassPathXmlApplicationContext("egovframework/spring/context-common.xml");

UserService userService =
    context.getBean("userService", UserService.class);
```

###### OpenRewrite 적용 가능 여부

```text
수정 불필요
```

---

##### (7) Component Annotation 유지

###### AS-IS

```java
@Service
public class UserService {

}
```

```java
@Repository
public class UserDAO {

}
```

```java
@Controller
public class UserController {

}
```

###### TO-BE

```java
@Service
public class UserService {

}
```

```java
@Repository
public class UserDAO {

}
```

```java
@Controller
public class UserController {

}
```

###### OpenRewrite 적용 가능 여부

```text
수정 불필요
```

---

##### (8) Component Scan 설정 유지

###### AS-IS

```xml
<context:component-scan base-package="egovframework" />
```

###### TO-BE

```xml
<context:component-scan base-package="egovframework" />
```

###### OpenRewrite 적용 가능 여부

```text
대부분 수정 불필요
```

###### 비고

설정은 유지 가능하지만, 전환 후 실제 Bean이 정상 스캔되는지 ApplicationContext 기동 테스트가 필요하다.

---

##### (9) Annotation Config 설정 유지

###### AS-IS

```xml
<context:annotation-config />
```

###### TO-BE

```xml
<context:annotation-config />
```

###### OpenRewrite 적용 가능 여부

```text
대부분 수정 불필요
```

###### 비고

`@Autowired`, `@Resource`, `@Value` 처리 여부를 함께 검증한다.

---

##### (10) XML Schema Location 검토

###### AS-IS

```xml
xsi:schemaLocation="
http://www.springframework.org/schema/context
http://www.springframework.org/schema/context/spring-context-3.2.xsd"
```

###### TO-BE

```xml
xsi:schemaLocation="
http://www.springframework.org/schema/context
http://www.springframework.org/schema/context/spring-context.xsd"
```

###### OpenRewrite 적용 가능 여부

```text
가능
```

###### 비고

버전 고정 XSD를 사용 중이면 버전 없는 XSD 경로로 정리하는 것이 좋다.

---

##### (11) Property Placeholder 설정 검토

###### AS-IS

```xml
<bean id="propertyConfigurer"
      class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
    <property name="locations">
        <list>
            <value>classpath:/egovframework/egovProps/globals.properties</value>
        </list>
    </property>
</bean>
```

###### TO-BE

```xml
<bean id="propertyConfigurer"
      class="org.springframework.context.support.PropertySourcesPlaceholderConfigurer">
    <property name="locations">
        <list>
            <value>classpath:/egovframework/egovProps/globals.properties</value>
        </list>
    </property>
</bean>
```

###### OpenRewrite 적용 가능 여부

```text
일부 가능
```

###### 비고

기존 `PropertyPlaceholderConfigurer`도 동작할 수 있으나 Spring 5.3 환경에서는 `PropertySourcesPlaceholderConfigurer` 사용을 검토할 수 있다.

---

##### (12) MessageSource 설정 유지

###### AS-IS

```xml
<bean id="messageSource"
      class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
    <property name="basenames">
        <list>
            <value>classpath:/egovframework/message/message-common</value>
        </list>
    </property>
    <property name="defaultEncoding" value="UTF-8"/>
</bean>
```

###### TO-BE

```xml
<bean id="messageSource"
      class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
    <property name="basenames">
        <list>
            <value>classpath:/egovframework/message/message-common</value>
        </list>
    </property>
    <property name="defaultEncoding" value="UTF-8"/>
</bean>
```

###### OpenRewrite 적용 가능 여부

```text
수정 불필요
```

###### 비고

설정 자체는 유지 가능하나, 화면 메시지, Validation 메시지, 다국어 메시지 출력 검증이 필요하다.

---

##### (13) Event Listener 사용 코드 검토

###### AS-IS

```java
@Component
public class SampleEventListener
        implements ApplicationListener<ContextRefreshedEvent> {

    @Override
    public void onApplicationEvent(ContextRefreshedEvent event) {

    }
}
```

###### TO-BE

```java
@Component
public class SampleEventListener
        implements ApplicationListener<ContextRefreshedEvent> {

    @Override
    public void onApplicationEvent(ContextRefreshedEvent event) {

    }
}
```

###### OpenRewrite 적용 가능 여부

```text
대부분 수정 불필요
```

###### 비고

일반적인 ApplicationListener 구현은 유지 가능하나, Context 초기화 시점에 의존하는 로직은 수동 검증이 필요하다.

---

##### (14) 커스텀 InitializingBean 검토

###### AS-IS

```java
public class CustomInitializer implements InitializingBean {

    @Override
    public void afterPropertiesSet() throws Exception {

    }
}
```

###### TO-BE

```java
public class CustomInitializer implements InitializingBean {

    @Override
    public void afterPropertiesSet() throws Exception {

    }
}
```

###### OpenRewrite 적용 가능 여부

```text
수정 불필요
```

###### 비고

초기화 로직이 DB, 파일, 외부 시스템에 의존하는 경우 전환 후 기동 시점 검증이 필요하다.

---

##### (15) OpenRewrite 적용 범위 정리

| 구분                                                    | 자동변환       |
| ----------------------------------------------------- | ---------- |
| `pom.xml` spring-context 버전 변경                        | 가능         |
| `WEB-INF/lib/spring-context-3.2.9.RELEASE.jar` 제거 목록화 | 가능         |
| Spring Framework 계열 버전 통일                             | 가능         |
| `ApplicationContext` 사용 코드                            | 수정 불필요     |
| Component Annotation 코드                               | 수정 불필요     |
| `<context:component-scan>`                            | 대부분 수정 불필요 |
| `<context:annotation-config>`                         | 대부분 수정 불필요 |
| XML Schema Location 정리                                | 가능         |
| `PropertyPlaceholderConfigurer` 변경                    | 일부 가능      |
| MessageSource 설정                                      | 수정 불필요     |
| Event Listener                                        | 대부분 수정 불필요 |
| ApplicationContext 기동 검증                              | 수동         |

---

##### (16) 최종 정리

```text
spring-context 3.2.9.RELEASE → 5.3.37 전환은 ApplicationContext 및 Bean 관리 구조의 버전 정합성 확보가 핵심이다.

일반적인 ApplicationContext 사용 코드, @Component, @Service, @Repository, @Controller,
<context:component-scan>, <context:annotation-config> 설정은 대부분 그대로 유지 가능하다.

OpenRewrite를 통해 Maven 의존성 변경, 구버전 Jar 탐지,
Spring Framework 계열 라이브러리 버전 통일, XML Schema Location 정리는 자동화할 수 있다.

다만 PropertyPlaceholderConfigurer, MessageSource, Event Listener,
커스텀 초기화 로직은 실제 ApplicationContext 기동 과정에서 동작 검증이 필요하다.

전환 후에는 ApplicationContext 기동 여부, Component Scan 정상 동작,
Property 주입, MessageSource 처리, Bean 초기화 로직을 반드시 검증해야 한다.
```
