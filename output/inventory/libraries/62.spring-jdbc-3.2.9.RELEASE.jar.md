# spring-jdbc-3.2.9.RELEASE.jar 분석 보고서

## 1. 기본 정보

| 항목       | 내용                             |
| -------- | ------------------------------ |
| 라이브러리명   | Spring JDBC                    |
| 파일명      | spring-jdbc-3.2.9.RELEASE.jar  |
| 버전       | 3.2.9.RELEASE                  |
| 제조사/오픈소스 | Spring Framework / Open Source |
| 라이선스     | Apache License 2.0             |
| 분류       | Spring JDBC Framework          |
| 전환 대상 여부 | 대상                             |
| 위험도      | 높음                             |

---

## 2. 라이브러리 개요

### 주요 기능

* JDBC Template 제공
* DataSource 관리
* SQL 실행 지원
* ResultSet 매핑 지원
* SQLException 예외 변환 지원

### 사용 목적

`spring-jdbc`는 JDBC 프로그래밍을 단순화하기 위한 Spring Framework 라이브러리이다.

반복적인 JDBC 코드(Connection, Statement, ResultSet 처리)를 제거하고 예외 처리 및 자원 해제를 자동화한다.

전자정부 프로젝트에서는 다음과 같은 경우에 사용된다.

* JdbcTemplate 사용
* NamedParameterJdbcTemplate 사용
* 직접 SQL 처리
* Batch 처리
* Stored Procedure 호출

### 대표 사용 예시

```java id="r9w4kt"
@Autowired
private JdbcTemplate jdbcTemplate;

public List<UserVO> selectUserList() {

    return jdbcTemplate.query(
        "SELECT * FROM TB_USER",
        new UserRowMapper()
    );
}
```

또는

```java id="m7u4ha"
int count =
    jdbcTemplate.queryForObject(
        "SELECT COUNT(*) FROM TB_USER",
        Integer.class
    );
```

---

## 3. AS-IS 사용 현황

### 프로젝트 내 사용 여부

* 확인 필요

### 확인 방법

```bash id="8l07n9"
find . -name "spring-jdbc*.jar"

grep -R "JdbcTemplate" src

grep -R "NamedParameterJdbcTemplate" src

grep -R "SimpleJdbcTemplate" src

grep -R "org.springframework.jdbc" src

grep -R "DataSource" src
```

### 예상 사용 위치

* DAO
* Service
* Batch
* 인터페이스 모듈
* 기타

---

## 4. eGovFrame 4.3 영향도 분석

### 전환 대상 여부

* 대상

### 전환 사유

`spring-jdbc-3.2.9.RELEASE.jar`는 Spring Framework 3.2 계열 JDBC 라이브러리이다.

eGovFrame 4.3은 Spring Framework 5.3.37 기반으로 구성되므로 Spring JDBC 라이브러리도 함께 업그레이드해야 한다.

특히 JDBC Template, Transaction, DataSource 설정은 Spring Core 계열과 밀접하게 연계되므로 버전 정합성 확보가 필요하다.

또한 프로젝트에서 iBATIS, MyBatis와 함께 사용 중인 경우 JDBC 관련 설정 검증이 필요하다.

### 영향 범위

| 구분      | 영향 |
| ------- | -- |
| 소스      | 높음 |
| 설정파일    | 높음 |
| pom.xml | 높음 |
| 운영환경    | 중간 |

---

## 5. 전환 시 검토사항

### 변경 필요 사항

* spring-jdbc 3.2.9 → 5.3.37 업그레이드
* Spring Framework 계열 라이브러리 버전 통일
* JdbcTemplate 사용 코드 검증
* DataSource 설정 검증
* Transaction 설정 검증
* Batch 처리 검증
* SQL 실행 결과 검증

### 자동 변환 가능 여부

| 구분          | 가능 여부 |
| ----------- | ----- |
| OpenRewrite | 일부 가능 |
| Python Rule | 가능    |
| LLM 보정      | 필요    |

### 예상 난이도

* 높음

---

## 6. 버전 업그레이드 검토

### AS-IS 버전

```text
spring-jdbc-3.2.9.RELEASE.jar
```

### TO-BE 버전

```text
spring-jdbc-5.3.37.jar
```

### 주요 변경사항

* Spring Framework 3.2 → 5.3 업그레이드
* JDBC Template 내부 구조 개선
* Batch 처리 기능 개선
* SQLException 처리 기능 개선
* JDK 8 이상 환경 적용
* Spring Framework 버전 정합성 확보

### 소스 수정 여부

* 일부 수정 필요

---

## 7. 위험요소

### 보안

* Spring 3.2 계열 기술지원 종료
* 최신 보안 패치 미적용
* Spring Framework 관련 공개 취약점 대응 불가

### 운영

* SQL 실행 오류 가능
* DataSource 초기화 실패 가능
* Transaction 처리 오류 가능
* Batch 프로그램 오류 가능
* DB 연결 오류 가능

### 호환성

* JDK 영향 있음
* Spring 영향 높음
* eGovFrame 영향 높음

---

## 8. 검토 결과

| 항목       | 결과       |
| -------- | -------- |
| 전환 대상 여부 | 대상       |
| 자동 변환    | 일부 가능    |
| 소스 수정    | 일부 수정 필요 |
| 설정 수정    | 일부 수정 필요 |
| 버전 업그레이드 | 필요       |
| 제거 가능성   | 없음       |
| 위험도      | 높음       |

---

## 9. 최종 판정

### 판정

```text
전환대상여부 :
대상

조치방안 :
spring-jdbc-3.2.9.RELEASE.jar 제거 후
spring-jdbc-5.3.37.jar 적용

spring-core
spring-beans
spring-context
spring-expression
spring-aop
spring-tx
등 Spring Framework 계열 라이브러리와 함께 버전 통일

JdbcTemplate 검증
DataSource 검증
Transaction 검증
SQL 실행 결과 검증 수행

자동변환 :
일부 가능

소스수정 :
일부 수정 필요

위험도 :
높음
```

### 비고

* eGovFrame 4.3 전환 시 반드시 업그레이드해야 하는 Spring Framework 핵심 라이브러리이다.
* `JdbcTemplate`, `NamedParameterJdbcTemplate`, `DataSource` 관련 기능은 대부분 그대로 사용 가능하나 실제 SQL 실행 검증이 필요하다.
* 전환 후 CRUD 기능, 배치 기능, Stored Procedure 기능에 대한 회귀 테스트를 수행해야 한다.
* 프로젝트에서 iBATIS 또는 MyBatis를 사용하더라도 Spring JDBC 기반 DataSource 및 Transaction 관리 기능은 계속 사용될 가능성이 높다.
* `spring-jdbc`는 `spring-tx`와 밀접하게 연계되므로 두 라이브러리는 함께 전환해야 한다.

---

#### (참고) spring-jdbc 3.2.9.RELEASE → 5.3.37 AS-IS / TO-BE 수정 가이드

##### (1) Java 소스 수정 여부 요약

| 구분                           |  수정 여부 | 내용                                                                  |
| ---------------------------- | -----: | ------------------------------------------------------------------- |
| `JdbcTemplate` 기본 사용         | 대부분 없음 | `query`, `update`, `queryForObject` 등 일반 사용은 유지 가능                  |
| `NamedParameterJdbcTemplate` | 대부분 없음 | 일반적인 Named Parameter SQL 사용은 유지 가능                                  |
| `RowMapper` 구현               | 대부분 없음 | `mapRow(ResultSet rs, int rowNum)` 구조 유지 가능                         |
| `ResultSetExtractor`         | 대부분 없음 | 일반적인 사용 방식 유지 가능                                                    |
| `PreparedStatementSetter`    | 대부분 없음 | 일반적인 파라미터 바인딩 유지 가능                                                 |
| `queryForInt()`              |  필요 가능 | 구형 메서드 사용 시 `queryForObject(..., Integer.class)`로 변경                |
| `queryForLong()`             |  필요 가능 | 구형 메서드 사용 시 `queryForObject(..., Long.class)`로 변경                   |
| `SimpleJdbcTemplate`         |     필요 | 제거/통합된 구형 API이므로 `JdbcTemplate` 또는 `NamedParameterJdbcTemplate`로 전환 |
| `DataSource` 설정              |  검토 필요 | DBCP/HikariCP 등 커넥션풀 변경 여부 확인                                       |
| Transaction 연계               |  검토 필요 | `spring-tx`, `DataSourceTransactionManager`와 함께 검증                  |

---

##### (2) 전환 대상 요약

| 항목                           | AS-IS                           | TO-BE                                         | OpenRewrite |
| ---------------------------- | ------------------------------- | --------------------------------------------- | ----------- |
| Maven 의존성                    | `spring-jdbc 3.2.9.RELEASE`     | `spring-jdbc 5.3.37`                          | 가능          |
| WEB-INF/lib                  | `spring-jdbc-3.2.9.RELEASE.jar` | `spring-jdbc-5.3.37.jar` 또는 Maven 관리          | 가능          |
| Spring 계열 버전                 | Spring 3.2.9 혼재                 | Spring 5.3.37 계열 통일                           | 가능          |
| `JdbcTemplate`               | 유지                              | 유지                                            | 대부분 수정 불필요  |
| `NamedParameterJdbcTemplate` | 유지                              | 유지                                            | 대부분 수정 불필요  |
| `RowMapper`                  | 유지                              | 유지                                            | 수정 불필요      |
| `queryForInt()`              | 구형 API                          | `queryForObject(sql, Integer.class)`          | 일부 가능       |
| `queryForLong()`             | 구형 API                          | `queryForObject(sql, Long.class)`             | 일부 가능       |
| `SimpleJdbcTemplate`         | 구형 API                          | `JdbcTemplate` / `NamedParameterJdbcTemplate` | 수동 검토       |
| `DataSource`                 | 기존 Pool                         | HikariCP 또는 현행 Pool 검토                        | 수동 검토       |
| Transaction                  | 기존 설정                           | Spring 5.3 환경 검증                              | 수동 검토       |

---

##### (3) Maven 의존성 변경

###### AS-IS

```xml
<dependency>
    <groupId>org.springframework</groupId>
    <artifactId>spring-jdbc</artifactId>
    <version>3.2.9.RELEASE</version>
</dependency>
```

###### TO-BE

```xml
<dependency>
    <groupId>org.springframework</groupId>
    <artifactId>spring-jdbc</artifactId>
    <version>5.3.37</version>
</dependency>
```

###### OpenRewrite 적용 가능 여부

```text
가능
```

---

##### (4) WEB-INF/lib 직접 Jar 변경

###### AS-IS

```text
WEB-INF/lib/spring-jdbc-3.2.9.RELEASE.jar
```

###### TO-BE

```text
WEB-INF/lib/spring-jdbc-5.3.37.jar
```

또는 Maven 기반 관리로 전환한다.

```xml
<dependency>
    <groupId>org.springframework</groupId>
    <artifactId>spring-jdbc</artifactId>
    <version>5.3.37</version>
</dependency>
```

###### OpenRewrite 적용 가능 여부

```text
가능
```

---

##### (5) Spring Framework 계열 버전 통일

###### AS-IS

```text
spring-core-3.2.9.RELEASE.jar
spring-beans-3.2.9.RELEASE.jar
spring-context-3.2.9.RELEASE.jar
spring-jdbc-3.2.9.RELEASE.jar
spring-tx-3.2.9.RELEASE.jar
```

###### TO-BE

```text
spring-core-5.3.37.jar
spring-beans-5.3.37.jar
spring-context-5.3.37.jar
spring-jdbc-5.3.37.jar
spring-tx-5.3.37.jar
```

###### OpenRewrite 적용 가능 여부

```text
가능
```

###### 비고

`spring-jdbc`는 `spring-tx`, `spring-core`, `spring-beans`와 함께 동작하므로 Spring Framework 계열 전체 버전을 동일하게 맞춰야 한다.

---

##### (6) JdbcTemplate 기본 사용 유지

###### AS-IS

```java
@Autowired
private JdbcTemplate jdbcTemplate;

public int updateUser(UserVO user) {
    return jdbcTemplate.update(
        "UPDATE TB_USER SET USER_NM = ? WHERE USER_ID = ?",
        user.getUserNm(),
        user.getUserId()
    );
}
```

###### TO-BE

```java
@Autowired
private JdbcTemplate jdbcTemplate;

public int updateUser(UserVO user) {
    return jdbcTemplate.update(
        "UPDATE TB_USER SET USER_NM = ? WHERE USER_ID = ?",
        user.getUserNm(),
        user.getUserId()
    );
}
```

###### OpenRewrite 적용 가능 여부

```text
수정 불필요
```

---

##### (7) RowMapper 구현 유지

###### AS-IS

```java
public class UserRowMapper implements RowMapper<UserVO> {

    @Override
    public UserVO mapRow(ResultSet rs, int rowNum)
            throws SQLException {

        UserVO user = new UserVO();
        user.setUserId(rs.getString("USER_ID"));
        user.setUserNm(rs.getString("USER_NM"));

        return user;
    }
}
```

###### TO-BE

```java
public class UserRowMapper implements RowMapper<UserVO> {

    @Override
    public UserVO mapRow(ResultSet rs, int rowNum)
            throws SQLException {

        UserVO user = new UserVO();
        user.setUserId(rs.getString("USER_ID"));
        user.setUserNm(rs.getString("USER_NM"));

        return user;
    }
}
```

###### OpenRewrite 적용 가능 여부

```text
수정 불필요
```

---

##### (8) queryForObject 사용 유지

###### AS-IS

```java
String userName =
    jdbcTemplate.queryForObject(
        "SELECT USER_NM FROM TB_USER WHERE USER_ID = ?",
        String.class,
        userId
    );
```

###### TO-BE

```java
String userName =
    jdbcTemplate.queryForObject(
        "SELECT USER_NM FROM TB_USER WHERE USER_ID = ?",
        String.class,
        userId
    );
```

###### OpenRewrite 적용 가능 여부

```text
수정 불필요
```

---

##### (9) queryForInt 변경

###### AS-IS

```java
int count =
    jdbcTemplate.queryForInt(
        "SELECT COUNT(*) FROM TB_USER"
    );
```

###### TO-BE

```java
int count =
    jdbcTemplate.queryForObject(
        "SELECT COUNT(*) FROM TB_USER",
        Integer.class
    );
```

###### OpenRewrite 적용 가능 여부

```text
일부 가능
```

###### 비고

`queryForInt()`는 구형 API이므로 `queryForObject(sql, Integer.class)`로 정리하는 것이 좋다.

---

##### (10) queryForLong 변경

###### AS-IS

```java
long count =
    jdbcTemplate.queryForLong(
        "SELECT COUNT(*) FROM TB_USER"
    );
```

###### TO-BE

```java
long count =
    jdbcTemplate.queryForObject(
        "SELECT COUNT(*) FROM TB_USER",
        Long.class
    );
```

###### OpenRewrite 적용 가능 여부

```text
일부 가능
```

---

##### (11) SimpleJdbcTemplate 전환

###### AS-IS

```java
@Autowired
private SimpleJdbcTemplate simpleJdbcTemplate;

public List<UserVO> selectUserList() {
    return simpleJdbcTemplate.query(
        "SELECT USER_ID, USER_NM FROM TB_USER",
        new UserRowMapper()
    );
}
```

###### TO-BE

```java
@Autowired
private JdbcTemplate jdbcTemplate;

public List<UserVO> selectUserList() {
    return jdbcTemplate.query(
        "SELECT USER_ID, USER_NM FROM TB_USER",
        new UserRowMapper()
    );
}
```

또는 Named Parameter 사용 시

```java
@Autowired
private NamedParameterJdbcTemplate namedParameterJdbcTemplate;
```

###### OpenRewrite 적용 가능 여부

```text
수동 검토
```

###### 비고

`SimpleJdbcTemplate`는 구형 API이므로 사용 흔적이 있으면 `JdbcTemplate` 또는 `NamedParameterJdbcTemplate`로 전환해야 한다.

---

##### (12) DataSource 설정 검토

###### AS-IS

```xml
<bean id="dataSource"
      class="org.apache.commons.dbcp.BasicDataSource">
    <property name="driverClassName" value="${Globals.DriverClassName}"/>
    <property name="url" value="${Globals.Url}"/>
    <property name="username" value="${Globals.UserName}"/>
    <property name="password" value="${Globals.Password}"/>
</bean>
```

###### TO-BE

```xml
<bean id="dataSource"
      class="com.zaxxer.hikari.HikariDataSource">
    <property name="driverClassName" value="${Globals.DriverClassName}"/>
    <property name="jdbcUrl" value="${Globals.Url}"/>
    <property name="username" value="${Globals.UserName}"/>
    <property name="password" value="${Globals.Password}"/>
</bean>
```

###### OpenRewrite 적용 가능 여부

```text
수동 검토
```

###### 비고

`spring-jdbc` 자체 변경이라기보다 커넥션풀 라이브러리 전환 이슈이다. 기존 DBCP를 유지할 수도 있으나 eGovFrame 4.3 환경에서는 커넥션풀 정비 여부를 함께 검토하는 것이 좋다.

---

##### (13) Transaction Manager 설정 유지

###### AS-IS

```xml
<bean id="transactionManager"
      class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
    <property name="dataSource" ref="dataSource"/>
</bean>
```

###### TO-BE

```xml
<bean id="transactionManager"
      class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
    <property name="dataSource" ref="dataSource"/>
</bean>
```

###### OpenRewrite 적용 가능 여부

```text
대부분 수정 불필요
```

###### 비고

설정은 대부분 유지 가능하지만, `spring-tx`, `DataSource`, 실제 commit/rollback 동작 검증이 필요하다.

---

##### (14) XML Schema Location 검토

###### AS-IS

```xml
xsi:schemaLocation="
http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
http://www.springframework.org/schema/tx
http://www.springframework.org/schema/tx/spring-tx-3.2.xsd"
```

###### TO-BE

```xml
xsi:schemaLocation="
http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans.xsd
http://www.springframework.org/schema/tx
http://www.springframework.org/schema/tx/spring-tx.xsd"
```

###### OpenRewrite 적용 가능 여부

```text
가능
```

---

##### (15) OpenRewrite 적용 범위 정리

| 구분                                                     | 자동변환       |
| ------------------------------------------------------ | ---------- |
| `pom.xml` spring-jdbc 버전 변경                            | 가능         |
| `WEB-INF/lib/spring-jdbc-3.2.9.RELEASE.jar` 제거 목록화     | 가능         |
| Spring Framework 계열 버전 통일                              | 가능         |
| `JdbcTemplate` 기본 사용 코드                                | 수정 불필요     |
| `RowMapper` 구현 코드                                      | 수정 불필요     |
| `queryForObject` 기본 사용                                 | 수정 불필요     |
| `queryForInt()` → `queryForObject(..., Integer.class)` | 일부 가능      |
| `queryForLong()` → `queryForObject(..., Long.class)`   | 일부 가능      |
| `SimpleJdbcTemplate` → `JdbcTemplate`                  | 수동 검토      |
| DataSource 커넥션풀 전환                                     | 수동 검토      |
| TransactionManager 설정                                  | 대부분 수정 불필요 |
| XML Schema Location 정리                                 | 가능         |
| SQL 실행 결과 검증                                           | 수동         |
| Commit/Rollback 검증                                     | 수동         |

---

##### (16) 최종 정리

```text
spring-jdbc 3.2.9.RELEASE → 5.3.37 전환은 JDBC 처리 기능 자체보다 Spring Framework 계열 버전 정합성과 DataSource/Transaction 연계 검증이 핵심이다.

일반적인 JdbcTemplate, NamedParameterJdbcTemplate, RowMapper, update, query, queryForObject 사용 코드는 대부분 그대로 유지 가능하다.

OpenRewrite를 통해 Maven 의존성 변경, 구버전 Jar 탐지,
Spring Framework 계열 라이브러리 버전 통일, XML Schema Location 정리,
queryForInt/queryForLong 일부 구형 API 치환은 자동화할 수 있다.

다만 SimpleJdbcTemplate 사용 코드, DataSource 커넥션풀 전환,
TransactionManager 연계, 실제 SQL 실행 결과 검증은 수동 검토가 필요하다.

전환 후에는 주요 CRUD, Batch SQL, Transaction commit/rollback,
DB 연결 및 커넥션풀 동작을 반드시 검증해야 한다.
```
