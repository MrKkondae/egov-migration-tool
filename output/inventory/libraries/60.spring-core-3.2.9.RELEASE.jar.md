# spring-core-3.2.9.RELEASE.jar 분석 보고서

## 1. 기본 정보

| 항목       | 내용                             |
| -------- | ------------------------------ |
| 라이브러리명   | Spring Core                    |
| 파일명      | spring-core-3.2.9.RELEASE.jar  |
| 버전       | 3.2.9.RELEASE                  |
| 제조사/오픈소스 | Spring Framework / Open Source |
| 라이선스     | Apache License 2.0             |
| 분류       | Spring Core Framework          |
| 전환 대상 여부 | 대상                             |
| 위험도      | 높음                             |

---

## 2. 라이브러리 개요

### 주요 기능

* Spring Framework 핵심 기능 제공
* Resource 로딩 기능 제공
* Reflection 및 Utility 기능 제공
* 타입 변환(Type Conversion) 지원
* Spring Container 기반 클래스 제공

### 사용 목적

`spring-core`는 Spring Framework의 가장 핵심적인 라이브러리로 다른 Spring 모듈들이 공통적으로 사용하는 기반 기능을 제공한다.

`spring-beans`, `spring-context`, `spring-aop`, `spring-tx`, `spring-web` 등 대부분의 Spring 라이브러리가 `spring-core`에 의존한다.

전자정부프레임워크 기반 시스템에서는 Spring Runtime 전체의 기반 라이브러리 역할을 수행한다.

### 대표 사용 예시

```java
import org.springframework.core.io.ClassPathResource;

ClassPathResource resource =
    new ClassPathResource("config.properties");
```

또는

```java
import org.springframework.util.StringUtils;

String value =
    StringUtils.trimWhitespace(text);
```

---

## 3. AS-IS 사용 현황

### 프로젝트 내 사용 여부

* 사용

### 확인 방법

```bash
find . -name "spring-core*.jar"

grep -R "org.springframework.core" src

grep -R "org.springframework.util" src

grep -R "ClassPathResource" src

grep -R "ResourceLoader" src
```

### 예상 사용 위치

* DAO
* Service
* Controller
* Batch
* 기타

---

## 4. eGovFrame 4.3 영향도 분석

### 전환 대상 여부

* 대상

### 전환 사유

`spring-core-3.2.9.RELEASE.jar`는 Spring Framework 3.2 계열의 핵심 라이브러리이다.

eGovFrame 4.3은 Spring Framework 5.3.37 기반으로 구성되므로 Spring Core 계열 라이브러리 전체 업그레이드가 필요하다.

특히 `spring-core`는 Spring Framework 전체의 기반 모듈이므로 버전 불일치가 발생할 경우 ApplicationContext 초기화 실패, Bean 생성 오류, Class Loading 오류 등이 발생할 수 있다.

따라서 eGovFrame 4.3 전환 시 반드시 업그레이드해야 하는 핵심 라이브러리이다.

### 영향 범위

| 구분      | 영향 |
| ------- | -- |
| 소스      | 높음 |
| 설정파일    | 중간 |
| pom.xml | 높음 |
| 운영환경    | 높음 |

---

## 5. 전환 시 검토사항

### 변경 필요 사항

* spring-core 3.2.9 → 5.3.37 업그레이드
* Spring Framework 계열 라이브러리 버전 통일
* Resource 로딩 기능 검증
* Utility 클래스 사용 여부 검증
* Reflection 기반 기능 검증
* ApplicationContext 기동 검증
* ClassLoader 관련 기능 검증

### 자동 변환 가능 여부

| 구분          | 가능 여부 |
| ----------- | ----- |
| OpenRewrite | 일부 가능 |
| Python Rule | 가능    |
| LLM 보정      | 필요    |

### 예상 난이도

* 높음

---

## 6. 버전 업그레이드 검토

### AS-IS 버전

```text
spring-core-3.2.9.RELEASE.jar
```

### TO-BE 버전

```text
spring-core-5.3.37.jar
```

### 주요 변경사항

* Spring Framework 3.2 → 5.3 업그레이드
* JDK 8 이상 환경 적용
* Resource 처리 기능 개선
* Reflection 처리 기능 개선
* Utility 기능 개선
* Spring Framework 내부 구조 개선

### 소스 수정 여부

* 일부 수정 필요

---

## 7. 위험요소

### 보안

* Spring 3.2 계열 기술지원 종료
* 최신 보안 패치 미적용
* Spring Framework 관련 공개 취약점 대응 불가

### 운영

* ApplicationContext 초기화 실패 가능
* Bean 생성 실패 가능
* Resource 로딩 오류 가능
* Spring 라이브러리 버전 충돌 가능
* Class Loading 오류 가능

### 호환성

* JDK 영향 있음
* Spring 영향 높음
* eGovFrame 영향 높음

---

## 8. 검토 결과

| 항목       | 결과       |
| -------- | -------- |
| 전환 대상 여부 | 대상       |
| 자동 변환    | 일부 가능    |
| 소스 수정    | 일부 수정 필요 |
| 설정 수정    | 일부 수정 필요 |
| 버전 업그레이드 | 필요       |
| 제거 가능성   | 없음       |
| 위험도      | 높음       |

---

## 9. 최종 판정

### 판정

```text
전환대상여부 :
대상

조치방안 :
spring-core-3.2.9.RELEASE.jar 제거 후
spring-core-5.3.37.jar 적용

spring-beans
spring-context
spring-expression
spring-aop
spring-tx
spring-web
등 Spring Framework 계열 라이브러리와 함께 버전 통일

ApplicationContext 기동 검증
Bean 생성 검증
Resource 로딩 검증 수행

자동변환 :
일부 가능

소스수정 :
일부 수정 필요

위험도 :
높음
```

### 비고

* eGovFrame 4.3 전환 시 반드시 업그레이드해야 하는 최우선 핵심 라이브러리이다.
* `spring-core`는 Spring Framework 전체의 기반 모듈이므로 영향 범위가 매우 넓다.
* 단독 전환이 아닌 Spring Core 계열(`spring-beans`, `spring-context`, `spring-expression`, `spring-aop`, `spring-tx`)과 함께 일괄 전환해야 한다.
* 일반적인 `org.springframework.util.*`, `org.springframework.core.*` 사용 코드는 대부분 유지 가능하나 컴파일 및 기동 테스트는 반드시 수행해야 한다.
* 전환 후 ApplicationContext 기동 테스트, 주요 업무 기능 테스트, 트랜잭션 및 DI 동작 검증을 수행해야 한다.

---

#### (참고) spring-core 3.2.9.RELEASE → 5.3.37 AS-IS / TO-BE 수정 가이드

##### (1) Java 소스 수정 여부 요약

| 구분                                     |  수정 여부 | 내용                          |
| -------------------------------------- | -----: | --------------------------- |
| `org.springframework.core.io.Resource` | 대부분 없음 | 일반적인 Resource 사용 코드는 유지 가능  |
| `ClassPathResource`                    | 대부분 없음 | 클래스패스 리소스 로딩 방식 유지 가능       |
| `FileSystemResource`                   | 대부분 없음 | 파일 시스템 리소스 로딩 방식 유지 가능      |
| `ResourceLoader`                       | 대부분 없음 | 일반적인 사용 방식은 유지 가능           |
| `StringUtils`                          |  검토 필요 | 일부 deprecated 메서드 사용 여부 확인  |
| `ObjectUtils`                          | 대부분 없음 | 일반적인 유틸 사용은 유지 가능           |
| `ReflectionUtils`                      |  검토 필요 | 내부 Reflection 처리 로직은 검증 필요  |
| `AnnotationUtils`                      |  검토 필요 | Annotation 메타정보 처리 방식 검증 필요 |
| `Ordered`, `PriorityOrdered`           | 대부분 없음 | 정렬 우선순위 인터페이스 유지 가능         |
| 구형 Spring 내부 API 직접 사용                 |  수정 가능 | 내부 클래스 의존 시 수동 보정 필요        |

---

##### (2) 전환 대상 요약

| 항목             | AS-IS                           | TO-BE                                | OpenRewrite |
| -------------- | ------------------------------- | ------------------------------------ | ----------- |
| Maven 의존성      | `spring-core 3.2.9.RELEASE`     | `spring-core 5.3.37`                 | 가능          |
| WEB-INF/lib    | `spring-core-3.2.9.RELEASE.jar` | `spring-core-5.3.37.jar` 또는 Maven 관리 | 가능          |
| Spring 계열 버전   | Spring 3.2.9 혼재                 | Spring 5.3.37 계열 통일                  | 가능          |
| Resource API   | `Resource`, `ClassPathResource` | 유지                                   | 수정 불필요      |
| Utility API    | `StringUtils`, `ObjectUtils`    | 유지, deprecated 확인                    | 일부 가능       |
| Reflection API | `ReflectionUtils`               | 유지, 동작 검증                            | 수동 검토       |
| Annotation API | `AnnotationUtils`               | 유지, 동작 검증                            | 수동 검토       |
| 내부 API 직접 사용   | 구형 내부 클래스 가능                    | 표준 API 전환 검토                         | 수동 검토       |
| XML Schema     | 버전 고정 XSD 가능                    | 버전 없는 XSD 권장                         | 가능          |

---

##### (3) Maven 의존성 변경

###### AS-IS

```xml
<dependency>
    <groupId>org.springframework</groupId>
    <artifactId>spring-core</artifactId>
    <version>3.2.9.RELEASE</version>
</dependency>
```

###### TO-BE

```xml
<dependency>
    <groupId>org.springframework</groupId>
    <artifactId>spring-core</artifactId>
    <version>5.3.37</version>
</dependency>
```

###### OpenRewrite 적용 가능 여부

```text
가능
```

---

##### (4) WEB-INF/lib 직접 Jar 변경

###### AS-IS

```text
WEB-INF/lib/spring-core-3.2.9.RELEASE.jar
```

###### TO-BE

```text
WEB-INF/lib/spring-core-5.3.37.jar
```

또는 Maven 기반 관리로 전환한다.

```xml
<dependency>
    <groupId>org.springframework</groupId>
    <artifactId>spring-core</artifactId>
    <version>5.3.37</version>
</dependency>
```

###### OpenRewrite 적용 가능 여부

```text
가능
```

---

##### (5) Spring Framework 계열 버전 통일

###### AS-IS

```text
spring-core-3.2.9.RELEASE.jar
spring-beans-3.2.9.RELEASE.jar
spring-context-3.2.9.RELEASE.jar
spring-expression-3.2.9.RELEASE.jar
spring-aop-3.2.9.RELEASE.jar
spring-tx-3.2.9.RELEASE.jar
spring-web-3.2.9.RELEASE.jar
```

###### TO-BE

```text
spring-core-5.3.37.jar
spring-beans-5.3.37.jar
spring-context-5.3.37.jar
spring-expression-5.3.37.jar
spring-aop-5.3.37.jar
spring-tx-5.3.37.jar
spring-web-5.3.37.jar
```

###### OpenRewrite 적용 가능 여부

```text
가능
```

###### 비고

`spring-core`는 Spring Framework 전체의 기반 라이브러리이므로 반드시 Spring 계열 라이브러리 전체 버전을 동일하게 맞춰야 한다.

---

##### (6) Resource 사용 코드 유지

###### AS-IS

```java
Resource resource =
    new ClassPathResource("egovframework/egovProps/globals.properties");
```

###### TO-BE

```java
Resource resource =
    new ClassPathResource("egovframework/egovProps/globals.properties");
```

###### OpenRewrite 적용 가능 여부

```text
수정 불필요
```

---

##### (7) ResourceLoader 사용 코드 유지

###### AS-IS

```java
@Autowired
private ResourceLoader resourceLoader;

Resource resource =
    resourceLoader.getResource("classpath:/egovframework/egovProps/globals.properties");
```

###### TO-BE

```java
@Autowired
private ResourceLoader resourceLoader;

Resource resource =
    resourceLoader.getResource("classpath:/egovframework/egovProps/globals.properties");
```

###### OpenRewrite 적용 가능 여부

```text
수정 불필요
```

---

##### (8) StringUtils 사용 코드 검토

###### AS-IS

```java
boolean empty =
    StringUtils.isEmpty(value);
```

###### TO-BE

```java
boolean empty =
    !StringUtils.hasLength(value);
```

또는

```java
boolean empty =
    !StringUtils.hasText(value);
```

###### OpenRewrite 적용 가능 여부

```text
일부 가능
```

###### 비고

`StringUtils.isEmpty()`는 Spring 5.3에서 사용 가능하더라도 deprecated 성격으로 볼 수 있으므로 `hasLength()` 또는 `hasText()`로 정리할 수 있다. 단, 의미가 다르므로 공백 문자열 처리 기준을 확인해야 한다.

---

##### (9) ObjectUtils 사용 코드 유지

###### AS-IS

```java
boolean empty =
    ObjectUtils.isEmpty(value);
```

###### TO-BE

```java
boolean empty =
    ObjectUtils.isEmpty(value);
```

###### OpenRewrite 적용 가능 여부

```text
수정 불필요
```

---

##### (10) ReflectionUtils 사용 코드 검토

###### AS-IS

```java
Field field =
    ReflectionUtils.findField(UserVO.class, "userId");

ReflectionUtils.makeAccessible(field);

Object value =
    ReflectionUtils.getField(field, userVO);
```

###### TO-BE

```java
Field field =
    ReflectionUtils.findField(UserVO.class, "userId");

ReflectionUtils.makeAccessible(field);

Object value =
    ReflectionUtils.getField(field, userVO);
```

###### OpenRewrite 적용 가능 여부

```text
수동 검토
```

###### 비고

일반적인 ReflectionUtils 사용은 유지 가능하나, private 필드 접근, 보안 정책, JDK 버전 영향은 실행 환경에서 검증해야 한다.

---

##### (11) AnnotationUtils 사용 코드 검토

###### AS-IS

```java
RequestMapping mapping =
    AnnotationUtils.findAnnotation(
        method,
        RequestMapping.class
    );
```

###### TO-BE

```java
RequestMapping mapping =
    AnnotationUtils.findAnnotation(
        method,
        RequestMapping.class
    );
```

###### OpenRewrite 적용 가능 여부

```text
수동 검토
```

###### 비고

Annotation 처리 로직은 대부분 유지 가능하나, Spring 5.3에서 메타 Annotation 처리 방식이나 MVC Annotation과 연계되는 경우 동작 검증이 필요하다.

---

##### (12) Ordered / PriorityOrdered 유지

###### AS-IS

```java
public class CustomProcessor implements Ordered {

    @Override
    public int getOrder() {
        return 1;
    }
}
```

###### TO-BE

```java
public class CustomProcessor implements Ordered {

    @Override
    public int getOrder() {
        return 1;
    }
}
```

###### OpenRewrite 적용 가능 여부

```text
수정 불필요
```

---

##### (13) 구형 내부 API 직접 사용 검토

###### AS-IS

```java
import org.springframework.core.LocalVariableTableParameterNameDiscoverer;
```

###### TO-BE

```java
import org.springframework.core.ParameterNameDiscoverer;
import org.springframework.core.DefaultParameterNameDiscoverer;
```

예시:

```java
ParameterNameDiscoverer discoverer =
    new DefaultParameterNameDiscoverer();
```

###### OpenRewrite 적용 가능 여부

```text
수동 검토
```

###### 비고

Spring 내부 구현 클래스나 특정 구현체에 직접 의존하는 경우 Spring 5.3 환경에서 동작 여부를 확인하고 표준 인터페이스 기반으로 전환하는 것이 좋다.

---

##### (14) XML Schema Location 검토

###### AS-IS

```xml
xsi:schemaLocation="
http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
http://www.springframework.org/schema/context
http://www.springframework.org/schema/context/spring-context-3.2.xsd"
```

###### TO-BE

```xml
xsi:schemaLocation="
http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans.xsd
http://www.springframework.org/schema/context
http://www.springframework.org/schema/context/spring-context.xsd"
```

###### OpenRewrite 적용 가능 여부

```text
가능
```

---

##### (15) OpenRewrite 적용 범위 정리

| 구분                                                 | 자동변환   |
| -------------------------------------------------- | ------ |
| `pom.xml` spring-core 버전 변경                        | 가능     |
| `WEB-INF/lib/spring-core-3.2.9.RELEASE.jar` 제거 목록화 | 가능     |
| Spring Framework 계열 버전 통일                          | 가능     |
| Resource 사용 코드                                     | 수정 불필요 |
| ResourceLoader 사용 코드                               | 수정 불필요 |
| `StringUtils.isEmpty()` 정리                         | 일부 가능  |
| `ObjectUtils` 사용 코드                                | 수정 불필요 |
| `ReflectionUtils` 사용 코드                            | 수동 검토  |
| `AnnotationUtils` 사용 코드                            | 수동 검토  |
| 내부 API 직접 사용                                       | 수동 검토  |
| XML Schema Location 정리                             | 가능     |
| ApplicationContext 기동 검증                           | 수동     |

---

##### (16) 최종 정리

```text
spring-core 3.2.9.RELEASE → 5.3.37 전환은 Spring Framework 전체 버전 정합성 확보가 핵심이다.

일반적인 Resource, ClassPathResource, ResourceLoader, ObjectUtils, Ordered 사용 코드는 대부분 그대로 유지 가능하다.

OpenRewrite를 통해 Maven 의존성 변경, 구버전 Jar 탐지,
Spring Framework 계열 라이브러리 버전 통일, XML Schema Location 정리는 자동화할 수 있다.

다만 StringUtils 일부 deprecated 사용, ReflectionUtils, AnnotationUtils,
Spring 내부 API 직접 사용 코드는 프로젝트별로 수동 검토가 필요하다.

전환 후에는 ApplicationContext 기동 여부, 리소스 로딩,
Bean 생성 및 주요 업무 기능 동작 여부를 반드시 검증해야 한다.
```
