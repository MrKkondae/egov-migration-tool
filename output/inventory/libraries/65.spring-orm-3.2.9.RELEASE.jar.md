# spring-orm-3.2.9.RELEASE.jar 분석 보고서

## 1. 기본 정보

| 항목       | 내용                             |
| -------- | ------------------------------ |
| 라이브러리명   | Spring ORM                     |
| 파일명      | spring-orm-3.2.9.RELEASE.jar   |
| 버전       | 3.2.9.RELEASE                  |
| 제조사/오픈소스 | Spring Framework / Open Source |
| 라이선스     | Apache License 2.0             |
| 분류       | ORM 연계 프레임워크                   |
| 전환 대상 여부 | 대상                             |
| 위험도      | 높음                             |

---

## 2. 라이브러리 개요

### 주요 기능

* ORM 프레임워크 연계 지원
* Hibernate 연동
* JPA 연동
* iBATIS 연동
* MyBatis-Spring 연동 지원
* ORM 트랜잭션 관리 지원

### 사용 목적

`spring-orm`은 Spring Framework와 ORM(Object Relational Mapping) 프레임워크를 연계하기 위한 라이브러리이다.

전자정부프레임워크 3.x 환경에서는 다음과 같은 구조에서 사용된다.

* Hibernate + Spring
* JPA + Spring
* iBATIS + Spring
* MyBatis + Spring
* LocalSessionFactoryBean 사용
* SqlMapClientFactoryBean 사용

특히 eGovFrame 3.x 계열의 iBATIS 기반 프로젝트에서는 `spring-orm`을 통해 SQL Mapper와 Spring을 연계하는 경우가 많다.

### 대표 사용 예시

#### iBATIS 연계

```xml id="b7y8sv"
<bean id="sqlMapClient"
      class="org.springframework.orm.ibatis.SqlMapClientFactoryBean">
</bean>
```

#### Hibernate 연계

```xml id="eh4mnz"
<bean id="sessionFactory"
      class="org.springframework.orm.hibernate3.LocalSessionFactoryBean">
</bean>
```

#### JPA 연계

```java id="jefuxm"
@PersistenceContext
private EntityManager entityManager;
```

---

## 3. AS-IS 사용 현황

### 프로젝트 내 사용 여부

* 사용 가능성 높음

### 확인 방법

```bash id="8nt6c2"
find . -name "spring-orm*.jar"

grep -R "org.springframework.orm" src

grep -R "SqlMapClientFactoryBean" src

grep -R "LocalSessionFactoryBean" src

grep -R "HibernateTemplate" src

grep -R "@PersistenceContext" src

grep -R "EntityManager" src
```

### 예상 사용 위치

* DAO
* Service
* 공통 데이터 접근 모듈
* ORM 설정 파일
* Transaction 설정

---

## 4. eGovFrame 4.3 영향도 분석

### 전환 대상 여부

* 대상

### 전환 사유

`spring-orm-3.2.9.RELEASE.jar`는 Spring Framework 3.2 기반 ORM 연계 라이브러리이다.

eGovFrame 4.3은 Spring Framework 5.3.37 기반이므로 Spring ORM 라이브러리도 함께 업그레이드가 필요하다.

특히 전자정부프레임워크 전환 시 가장 중요한 검토 대상 중 하나는 다음과 같다.

* iBATIS 사용 여부
* MyBatis 사용 여부
* Hibernate 사용 여부
* JPA 사용 여부

만약 iBATIS를 사용 중인 경우에는 eGovFrame 4.3 전환 과정에서 MyBatis 전환 검토가 필요할 수 있으며, `spring-orm` 설정 역시 영향을 받을 수 있다.

### 영향 범위

| 구분      | 영향 |
| ------- | -- |
| 소스      | 높음 |
| 설정파일    | 높음 |
| pom.xml | 높음 |
| 운영환경    | 중간 |

---

## 5. 전환 시 검토사항

### 변경 필요 사항

* spring-orm 3.2.9 → 5.3.37 업그레이드
* Spring Framework 계열 라이브러리 버전 통일
* ORM 연계 구조 분석
* iBATIS 사용 여부 확인
* MyBatis 사용 여부 확인
* Hibernate 사용 여부 확인
* JPA 사용 여부 확인
* Transaction 설정 검증

### 자동 변환 가능 여부

| 구분          | 가능 여부 |
| ----------- | ----- |
| OpenRewrite | 일부 가능 |
| Python Rule | 일부 가능 |
| LLM 보정      | 필요    |

### 예상 난이도

* 높음

---

## 6. 버전 업그레이드 검토

### AS-IS 버전

```text id="pvvbwd"
spring-orm-3.2.9.RELEASE.jar
```

### TO-BE 버전

```text id="onh1pq"
spring-orm-5.3.37.jar
```

### 주요 변경사항

* Spring Framework 3.2 → 5.3 업그레이드
* ORM 연계 기능 최신화
* JPA 지원 기능 개선
* Hibernate 연계 기능 개선
* Spring Framework 버전 정합성 확보

### 소스 수정 여부

* 일부 수정 필요

---

## 7. 위험요소

### 보안

* Spring 3.2 계열 기술지원 종료
* 최신 보안 패치 미적용
* Spring Framework 관련 공개 취약점 대응 불가

### 운영

* DAO 동작 오류 가능
* ORM 매핑 오류 가능
* Transaction 처리 오류 가능
* SessionFactory 초기화 실패 가능
* EntityManager 초기화 실패 가능

### 호환성

* JDK 영향 있음
* Spring 영향 높음
* eGovFrame 영향 높음

---

## 8. 검토 결과

| 항목       | 결과       |
| -------- | -------- |
| 전환 대상 여부 | 대상       |
| 자동 변환    | 일부 가능    |
| 소스 수정    | 일부 수정 필요 |
| 설정 수정    | 일부 수정 필요 |
| 버전 업그레이드 | 필요       |
| 제거 가능성   | 낮음       |
| 위험도      | 높음       |

---

## 9. 최종 판정

### 판정

```text id="2zb6eh"
전환대상여부 :
대상

조치방안 :
spring-orm-3.2.9.RELEASE.jar 제거 후
spring-orm-5.3.37.jar 적용

spring-core
spring-beans
spring-context
spring-expression
spring-aop
spring-jdbc
spring-tx
등 Spring Framework 계열 라이브러리와 함께 버전 통일

ORM 연계 구조 분석 수행

iBATIS 사용 시
MyBatis 전환 영향도 분석 수행

Hibernate/JPA 사용 시
ORM 설정 검증 수행

자동변환 :
일부 가능

소스수정 :
일부 수정 필요

위험도 :
높음
```

### 비고

* eGovFrame 3.x → 4.3 전환 시 데이터 접근 계층(DAO) 분석의 핵심 라이브러리 중 하나이다.
* 실제 프로젝트에서 `SqlMapClientFactoryBean`, `HibernateTemplate`, `LocalSessionFactoryBean`, `EntityManager` 사용 여부를 우선 확인해야 한다.
* 사용 중인 ORM 기술(iBATIS, MyBatis, Hibernate, JPA)에 따라 전환 난이도가 크게 달라진다.
* 특히 사용자의 전환 프로젝트처럼 **eGovFrame 3.x → 4.3 전환 시 iBATIS → MyBatis 전환 여부는 별도 영향도 분석 대상**으로 관리하는 것이 바람직하다.
* CRUD 기능, 트랜잭션 처리, SQL Mapper 동작에 대한 회귀 테스트를 반드시 수행해야 한다.

