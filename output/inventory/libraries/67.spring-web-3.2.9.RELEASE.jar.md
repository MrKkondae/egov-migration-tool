# spring-web-3.2.9.RELEASE.jar 분석 보고서

## 1. 기본 정보

| 항목       | 내용                             |
| -------- | ------------------------------ |
| 라이브러리명   | Spring Web                     |
| 파일명      | spring-web-3.2.9.RELEASE.jar   |
| 버전       | 3.2.9.RELEASE                  |
| 제조사/오픈소스 | Spring Framework / Open Source |
| 라이선스     | Apache License 2.0             |
| 분류       | 웹 공통 프레임워크                     |
| 전환 대상 여부 | 대상                             |
| 위험도      | 높음                             |

---

## 2. 라이브러리 개요

### 주요 기능

* 웹 애플리케이션 공통 기능 제공
* Servlet 연계 지원
* Multipart File Upload 지원
* WebApplicationContext 제공
* REST 연계 지원
* HTTP 요청/응답 처리 지원

### 사용 목적

`spring-web`은 Spring Framework의 웹 계층 공통 기능을 제공하는 라이브러리이다.

Spring MVC의 핵심 기능은 `spring-webmvc`에서 제공하지만, 웹 환경 구동을 위한 기반 기능은 `spring-web`에서 제공한다.

전자정부 프로젝트에서는 다음과 같은 기능에서 사용된다.

* Controller 처리
* File Upload 처리
* Multipart Request 처리
* REST API 처리
* WebApplicationContext 초기화
* HTTP 요청 및 응답 처리

### 대표 사용 예시

#### MultipartFile 사용

```java id="5k3yxa"
public String upload(
    MultipartFile file
) {

    String fileName =
        file.getOriginalFilename();

    return fileName;
}
```

#### RestTemplate 사용

```java id="q6n7pd"
RestTemplate restTemplate =
    new RestTemplate();

String result =
    restTemplate.getForObject(
        url,
        String.class
    );
```

---

## 3. AS-IS 사용 현황

### 프로젝트 내 사용 여부

* 사용

### 확인 방법

```bash id="pwf5s2"
find . -name "spring-web*.jar"

grep -R "MultipartFile" src

grep -R "RestTemplate" src

grep -R "WebApplicationContext" src

grep -R "org.springframework.web" src

grep -R "MultipartResolver" src
```

### 예상 사용 위치

* Controller
* Service
* 파일 업로드 모듈
* REST 연계 모듈
* 인터페이스 모듈
* 공통 웹 모듈

---

## 4. eGovFrame 4.3 영향도 분석

### 전환 대상 여부

* 대상

### 전환 사유

`spring-web-3.2.9.RELEASE.jar`는 Spring Framework 3.2 기반 웹 라이브러리이다.

eGovFrame 4.3은 Spring Framework 5.3.37 기반으로 구성되므로 `spring-web` 역시 반드시 업그레이드해야 한다.

특히 `spring-web`은 다음 라이브러리와 밀접하게 연관된다.

* spring-webmvc
* spring-core
* spring-context
* spring-beans
* spring-expression

전자정부프레임워크 4.3 전환 시 웹 계층 전반에 영향을 주는 핵심 라이브러리이므로 영향도가 높다.

### 영향 범위

| 구분      | 영향 |
| ------- | -- |
| 소스      | 높음 |
| 설정파일    | 높음 |
| pom.xml | 높음 |
| 운영환경    | 높음 |

---

## 5. 전환 시 검토사항

### 변경 필요 사항

* spring-web 3.2.9 → 5.3.37 업그레이드
* Spring Framework 계열 라이브러리 버전 통일
* Multipart Upload 기능 검증
* RestTemplate 사용 여부 검증
* HTTP Message Converter 검증
* WebApplicationContext 초기화 검증
* 인터페이스 연계 기능 검증
* 파일 업로드 기능 검증

### 자동 변환 가능 여부

| 구분          | 가능 여부 |
| ----------- | ----- |
| OpenRewrite | 일부 가능 |
| Python Rule | 가능    |
| LLM 보정      | 필요    |

### 예상 난이도

* 높음

---

## 6. 버전 업그레이드 검토

### AS-IS 버전

```text id="2gjmyc"
spring-web-3.2.9.RELEASE.jar
```

### TO-BE 버전

```text id="k3w6fr"
spring-web-5.3.37.jar
```

### 주요 변경사항

* Spring Framework 3.2 → 5.3 업그레이드
* REST 처리 기능 개선
* Multipart 처리 기능 개선
* HTTP Message Converter 개선
* WebApplicationContext 기능 개선
* Spring Framework 버전 정합성 확보

### 소스 수정 여부

* 일부 수정 필요

---

## 7. 위험요소

### 보안

* Spring 3.2 계열 기술지원 종료
* 최신 보안 패치 미적용
* Spring Framework 관련 공개 취약점 대응 불가

### 운영

* 파일 업로드 오류 가능
* REST 연계 오류 가능
* HTTP 요청 처리 오류 가능
* WebApplicationContext 초기화 실패 가능
* 인터페이스 연계 오류 가능

### 호환성

* JDK 영향 있음
* Spring 영향 높음
* eGovFrame 영향 높음

---

## 8. 검토 결과

| 항목       | 결과       |
| -------- | -------- |
| 전환 대상 여부 | 대상       |
| 자동 변환    | 일부 가능    |
| 소스 수정    | 일부 수정 필요 |
| 설정 수정    | 일부 수정 필요 |
| 버전 업그레이드 | 필요       |
| 제거 가능성   | 없음       |
| 위험도      | 높음       |

---

## 9. 최종 판정

### 판정

```text id="x4w2s8"
전환대상여부 :
대상

조치방안 :
spring-web-3.2.9.RELEASE.jar 제거 후
spring-web-5.3.37.jar 적용

spring-core
spring-beans
spring-context
spring-expression
spring-webmvc
등 Spring Framework 계열 라이브러리와 함께 버전 통일

Multipart Upload 검증
REST 연계 기능 검증
WebApplicationContext 초기화 검증
HTTP 요청/응답 처리 검증 수행

자동변환 :
일부 가능

소스수정 :
일부 수정 필요

위험도 :
높음
```

### 비고

* eGovFrame 4.3 전환 시 반드시 업그레이드해야 하는 Spring Framework 핵심 라이브러리이다.
* `spring-web`은 `spring-webmvc`의 기반 라이브러리 역할을 수행하므로 두 라이브러리는 함께 전환해야 한다.
* `MultipartFile`, `MultipartResolver`, `RestTemplate`, `HttpMessageConverter` 사용 여부를 우선 확인해야 한다.
* 파일 업로드, 다운로드, REST API 연계, 외부 시스템 인터페이스 기능은 집중 검증 대상이다.
* 전환 후 파일 업로드 기능, 인터페이스 연계 기능, REST 서비스 기능에 대한 회귀 테스트를 반드시 수행해야 한다.

---

#### (참고) spring-web 3.2.9.RELEASE → 5.3.37 AS-IS / TO-BE 수정 가이드

##### (1) Java 소스 수정 여부 요약

| 구분                                    |    수정 여부 | 내용                             |
| ------------------------------------- | -------: | ------------------------------ |
| `MultipartFile` 사용                    |   대부분 없음 | 일반적인 파일 업로드 코드는 유지 가능          |
| `MultipartHttpServletRequest`         |   대부분 없음 | 기존 Multipart 요청 처리 방식 유지 가능    |
| `RestTemplate` 사용                     |   대부분 없음 | 일반적인 REST 호출 코드는 유지 가능         |
| `HttpEntity`, `ResponseEntity`        |   대부분 없음 | HTTP 요청/응답 래퍼 사용 유지 가능         |
| `HttpHeaders`                         |   대부분 없음 | 헤더 설정 방식 유지 가능                 |
| `UriComponentsBuilder`                |   대부분 없음 | URI 생성 방식 유지 가능                |
| `WebApplicationContext`               |   대부분 없음 | 웹 컨텍스트 사용 방식 유지 가능             |
| `ContextLoaderListener`               |   대부분 없음 | web.xml 설정 구조 유지 가능            |
| `CommonsMultipartResolver`            |    검토 필요 | commons-fileupload 버전 영향 확인 필요 |
| `StringHttpMessageConverter`          |    검토 필요 | 문자셋/한글 응답 인코딩 확인 필요            |
| `MappingJackson2HttpMessageConverter` |    검토 필요 | Jackson 2.x 버전 정합성 확인 필요       |
| 구형 `AsyncRestTemplate`                | 수정 필요 가능 | 사용 시 대체 검토 필요                  |

---

##### (2) 전환 대상 요약

| 항목                       | AS-IS                          | TO-BE                               | OpenRewrite |
| ------------------------ | ------------------------------ | ----------------------------------- | ----------- |
| Maven 의존성                | `spring-web 3.2.9.RELEASE`     | `spring-web 5.3.37`                 | 가능          |
| WEB-INF/lib              | `spring-web-3.2.9.RELEASE.jar` | `spring-web-5.3.37.jar` 또는 Maven 관리 | 가능          |
| Spring 계열 버전             | Spring 3.2.9 혼재                | Spring 5.3.37 계열 통일                 | 가능          |
| Multipart API            | `MultipartFile`                | 유지, 업로드 검증                          | 대부분 수정 불필요  |
| RestTemplate             | 기존 REST 호출                     | 유지, 응답 검증                           | 대부분 수정 불필요  |
| HTTP Message Converter   | 기존 Converter                   | Jackson/문자셋 검증                      | 수동 검토       |
| ContextLoaderListener    | 기존 web.xml                     | 유지, 기동 검증                           | 대부분 수정 불필요  |
| CommonsMultipartResolver | 기존 설정                          | commons-fileupload 버전 검토            | 수동 검토       |
| XML Schema               | 버전 고정 XSD 가능                   | 버전 없는 XSD 권장                        | 가능          |

---

##### (3) Maven 의존성 변경

###### AS-IS

```xml
<dependency>
    <groupId>org.springframework</groupId>
    <artifactId>spring-web</artifactId>
    <version>3.2.9.RELEASE</version>
</dependency>
```

###### TO-BE

```xml
<dependency>
    <groupId>org.springframework</groupId>
    <artifactId>spring-web</artifactId>
    <version>5.3.37</version>
</dependency>
```

###### OpenRewrite 적용 가능 여부

```text
가능
```

---

##### (4) WEB-INF/lib 직접 Jar 변경

###### AS-IS

```text
WEB-INF/lib/spring-web-3.2.9.RELEASE.jar
```

###### TO-BE

```text
WEB-INF/lib/spring-web-5.3.37.jar
```

또는 Maven 기반 관리로 전환한다.

```xml
<dependency>
    <groupId>org.springframework</groupId>
    <artifactId>spring-web</artifactId>
    <version>5.3.37</version>
</dependency>
```

###### OpenRewrite 적용 가능 여부

```text
가능
```

---

##### (5) Spring Framework 계열 버전 통일

###### AS-IS

```text
spring-core-3.2.9.RELEASE.jar
spring-beans-3.2.9.RELEASE.jar
spring-context-3.2.9.RELEASE.jar
spring-web-3.2.9.RELEASE.jar
spring-webmvc-3.2.9.RELEASE.jar
```

###### TO-BE

```text
spring-core-5.3.37.jar
spring-beans-5.3.37.jar
spring-context-5.3.37.jar
spring-web-5.3.37.jar
spring-webmvc-5.3.37.jar
```

###### OpenRewrite 적용 가능 여부

```text
가능
```

###### 비고

`spring-web`은 `spring-webmvc`의 기반 모듈이므로 Spring MVC 계열과 함께 동일 버전으로 맞춰야 한다.

---

##### (6) MultipartFile 사용 코드 유지

###### AS-IS

```java
@PostMapping("/file/upload.do")
public String uploadFile(MultipartFile file) throws Exception {

    String originalFileName = file.getOriginalFilename();

    return originalFileName;
}
```

###### TO-BE

```java
@PostMapping("/file/upload.do")
public String uploadFile(MultipartFile file) throws Exception {

    String originalFileName = file.getOriginalFilename();

    return originalFileName;
}
```

###### OpenRewrite 적용 가능 여부

```text
수정 불필요
```

###### 비고

일반적인 `MultipartFile` 사용 코드는 대부분 유지 가능하다. 단, 실제 업로드 용량 제한, 임시 디렉터리, 한글 파일명 처리는 검증해야 한다.

---

##### (7) MultipartHttpServletRequest 사용 코드 유지

###### AS-IS

```java
public String upload(
        MultipartHttpServletRequest request) throws Exception {

    Iterator<String> fileNames =
        request.getFileNames();

    MultipartFile file =
        request.getFile(fileNames.next());

    return file.getOriginalFilename();
}
```

###### TO-BE

```java
public String upload(
        MultipartHttpServletRequest request) throws Exception {

    Iterator<String> fileNames =
        request.getFileNames();

    MultipartFile file =
        request.getFile(fileNames.next());

    return file.getOriginalFilename();
}
```

###### OpenRewrite 적용 가능 여부

```text
수정 불필요
```

---

##### (8) CommonsMultipartResolver 설정 검토

###### AS-IS

```xml
<bean id="multipartResolver"
      class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
    <property name="maxUploadSize" value="104857600"/>
    <property name="defaultEncoding" value="UTF-8"/>
</bean>
```

###### TO-BE

```xml
<bean id="multipartResolver"
      class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
    <property name="maxUploadSize" value="104857600"/>
    <property name="defaultEncoding" value="UTF-8"/>
</bean>
```

###### OpenRewrite 적용 가능 여부

```text
대부분 수정 불필요
```

###### 비고

설정은 유지 가능하지만 `commons-fileupload`, `commons-io` 버전과 함께 검증해야 한다. Servlet 3 기반 업로드로 전환하는 경우 별도 검토가 필요하다.

---

##### (9) RestTemplate 사용 코드 유지

###### AS-IS

```java
RestTemplate restTemplate =
    new RestTemplate();

String result =
    restTemplate.getForObject(
        "https://api.example.com/users/{userId}",
        String.class,
        userId
    );
```

###### TO-BE

```java
RestTemplate restTemplate =
    new RestTemplate();

String result =
    restTemplate.getForObject(
        "https://api.example.com/users/{userId}",
        String.class,
        userId
    );
```

###### OpenRewrite 적용 가능 여부

```text
수정 불필요
```

###### 비고

일반적인 `RestTemplate` 사용은 유지 가능하다. 단, SSL/TLS, timeout, MessageConverter, Jackson 버전은 검증해야 한다.

---

##### (10) HttpEntity / ResponseEntity 사용 코드 유지

###### AS-IS

```java
HttpHeaders headers = new HttpHeaders();
headers.setContentType(MediaType.APPLICATION_JSON);

HttpEntity<String> entity =
    new HttpEntity<>(jsonBody, headers);

ResponseEntity<String> response =
    restTemplate.postForEntity(url, entity, String.class);
```

###### TO-BE

```java
HttpHeaders headers = new HttpHeaders();
headers.setContentType(MediaType.APPLICATION_JSON);

HttpEntity<String> entity =
    new HttpEntity<>(jsonBody, headers);

ResponseEntity<String> response =
    restTemplate.postForEntity(url, entity, String.class);
```

###### OpenRewrite 적용 가능 여부

```text
수정 불필요
```

---

##### (11) HTTP Message Converter 검토

###### AS-IS

```xml
<bean class="org.springframework.http.converter.StringHttpMessageConverter"/>

<bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter"/>
```

###### TO-BE

```xml
<bean class="org.springframework.http.converter.StringHttpMessageConverter"/>

<bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter"/>
```

###### OpenRewrite 적용 가능 여부

```text
수동 검토
```

###### 비고

클래스명은 유지 가능하나 한글 인코딩, JSON 변환, Jackson 2.x 버전 정합성을 반드시 확인해야 한다.

---

##### (12) WebApplicationContext / ContextLoaderListener 설정 유지

###### AS-IS

```xml
<listener>
    <listener-class>
        org.springframework.web.context.ContextLoaderListener
    </listener-class>
</listener>
```

###### TO-BE

```xml
<listener>
    <listener-class>
        org.springframework.web.context.ContextLoaderListener
    </listener-class>
</listener>
```

###### OpenRewrite 적용 가능 여부

```text
수정 불필요
```

###### 비고

web.xml 구조는 유지 가능하지만 Root WebApplicationContext 기동 여부는 검증해야 한다.

---

##### (13) XML Schema Location 검토

###### AS-IS

```xml
xsi:schemaLocation="
http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
http://www.springframework.org/schema/context
http://www.springframework.org/schema/context/spring-context-3.2.xsd"
```

###### TO-BE

```xml
xsi:schemaLocation="
http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans.xsd
http://www.springframework.org/schema/context
http://www.springframework.org/schema/context/spring-context.xsd"
```

###### OpenRewrite 적용 가능 여부

```text
가능
```

---

##### (14) AsyncRestTemplate 사용 코드 검토

###### AS-IS

```java
AsyncRestTemplate asyncRestTemplate =
    new AsyncRestTemplate();
```

###### TO-BE

```java
RestTemplate restTemplate =
    new RestTemplate();
```

또는 비동기 호출이 필요한 경우 별도 비동기 처리 구조를 검토한다.

###### OpenRewrite 적용 가능 여부

```text
수동 검토
```

###### 비고

`AsyncRestTemplate` 사용 흔적이 있으면 Spring 5.3 환경에서 유지 여부를 검토하고 대체 방안을 수립해야 한다.

---

##### (15) OpenRewrite 적용 범위 정리

| 구분                                                | 자동변환       |
| ------------------------------------------------- | ---------- |
| `pom.xml` spring-web 버전 변경                        | 가능         |
| `WEB-INF/lib/spring-web-3.2.9.RELEASE.jar` 제거 목록화 | 가능         |
| Spring Framework 계열 버전 통일                         | 가능         |
| `MultipartFile` 사용 코드                             | 수정 불필요     |
| `MultipartHttpServletRequest` 사용 코드               | 수정 불필요     |
| `CommonsMultipartResolver` 설정                     | 대부분 수정 불필요 |
| `RestTemplate` 기본 사용 코드                           | 수정 불필요     |
| `HttpEntity`, `ResponseEntity` 사용 코드              | 수정 불필요     |
| HTTP Message Converter 설정                         | 수동 검토      |
| `ContextLoaderListener` 설정                        | 수정 불필요     |
| XML Schema Location 정리                            | 가능         |
| `AsyncRestTemplate` 사용 코드                         | 수동 검토      |
| 파일 업로드/다운로드 검증                                    | 수동         |
| REST 연계 검증                                        | 수동         |

---

##### (16) 최종 정리

```text
spring-web 3.2.9.RELEASE → 5.3.37 전환은 웹 공통 기능의 버전 정합성과 파일 업로드, REST 연계, HTTP Message Converter 검증이 핵심이다.

일반적인 MultipartFile, MultipartHttpServletRequest, RestTemplate,
HttpEntity, ResponseEntity, ContextLoaderListener 사용 코드는 대부분 그대로 유지 가능하다.

OpenRewrite를 통해 Maven 의존성 변경, 구버전 Jar 탐지,
Spring Framework 계열 라이브러리 버전 통일, XML Schema Location 정리는 자동화할 수 있다.

다만 CommonsMultipartResolver는 commons-fileupload 버전과 함께 검토해야 하며,
HTTP Message Converter는 Jackson 2.x 및 문자셋 설정과 함께 검증해야 한다.

전환 후에는 파일 업로드/다운로드, 한글 파일명, REST API 연계,
JSON 변환, WebApplicationContext 기동 여부를 반드시 검증해야 한다.
```
